Install by running the following in directory:

 - npm install
 - node server.js

Then access through browser using 'localhost:8080' !
For best experience open on multiple tabs/windows to
see how users experience Sketchy.